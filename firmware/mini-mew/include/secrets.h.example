#pragma once

// Copy to secrets.h and fill in. secrets.h is gitignored.

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

// Your public HTTPS relay (no https:// prefix)
#define TOUCH_HOST    "touch.example.com"
#define TOUCH_PORT    443
#define TOUCH_PATH    "/touch"
